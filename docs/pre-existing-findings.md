# Pre-Existing Findings Catalog — SUPERSEDED

> **Superseded 2026-06-05** by `docs/implementation-artifacts/deferred-work.md`.

The catalog protocol moved to colocate the discipline with the BMad `bmad-quick-dev` skill's append target (`step-04-review.md`, `step-oneshot.md`, etc.). The OPEN entries `PF-001..PF-004` that lived here were ported to the new file with their IDs preserved, so historical story-spec references like `[Source: docs/pre-existing-findings.md#PF-004]` still resolve by ID in the new location.

**Do not add new entries here.** See `docs/implementation-artifacts/deferred-work.md` for the active catalog, the catalog protocol (`## How to add an entry`), and all current OPEN / WONT-FIX entries.

To retrieve any historical entry previously closed under this file: `git log -p -- docs/pre-existing-findings.md` and search by finding ID (`PF-`, `ST-`, `CD-`, `CQ-`, `TF-`, `TQ-`).
